I have uploaded all the required codes that i have used in this Project. The trained .pth files have not been uploaded because of the size limitation.
The check_gpu program allows you to check if your system has a GPU and if the CUDA is active. CUDA helps you speed up the training process by using your GPU. 
In out project, we have made 3 models and compared them for efficienty. The files of the 3 models are p1, p2 and p3. We have one preprocessing module. 
Realtime.py is the file that runs the while program.
